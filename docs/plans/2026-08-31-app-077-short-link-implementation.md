# App 077 · ROUTE/77 实施计划

## Step 1：领域核心与存储边界

- 输出：`route-core.js`，提供 URL/别名校验、短链实体、访问记录和统计聚合。
- 测试：Node 单测覆盖合法、非法、重复和聚合边界。

## Step 2：静态产品界面

- 输出：响应式线路调度台，可创建、暂停、删除、搜索、复制、导出并生成二维码。
- 测试：脚本语法检查与浏览器交互验收；刷新后本地数据仍存在。

## Step 3：可选 Node 服务

- 输出：零依赖 HTTP 服务、真实 `/r/:slug` 重定向、JSON 持久化和 REST API。
- 测试：服务 API、跳转计数、状态码、请求限制和重启恢复。

## Step 4：发布与追踪器

- 输出：README、截图、根追踪器 77 号官方条目及回归测试。
- 测试：项目全套测试、tracker 测试、桌面与手机浏览器截图检查。

## Step 5：同步

- 输出：吸收 `origin/main` 最新并行项目状态，提交并仅推送 GitHub `origin`。
- 测试：本地 HEAD、`origin/main` 与推送提交一致，工作区干净。
