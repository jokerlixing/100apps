# #038 PULSE/38 在线问卷系统

一个面向工作坊主持人、产品研究者和小团队的零依赖问卷工具。从主题、参考文本和提示词生成完整问卷，本地模式可按内容自动匹配题型，再校对、编辑、分享和查看本地结果。

[在线使用](https://jokerlixing.github.io/100apps/apps/038-online-survey/)。本地模式无账号、无后端、无外部依赖；联网 AI 模式由用户自行配置兼容服务。

## 功能

- **文本生成问卷**：输入调查主题、参考文本、提示词与受访人群，填写 4–30 范围内的任意整数题数，生成标题、说明与完整题目
- **按内容自动匹配题型**：默认从编号提纲、字段名称、完整问题或自然语言清单推断题目和题型，无需逐题加标签；也可切换为手动限定题型
- **完整保留长文本**：参考文本与提示词不设置应用级字符硬上限，保存成功后刷新可恢复完整输入
- **两种生成方式**：默认本地规则与模板生成，也可选择用户配置的 AI 服务理解较复杂的文本要求
- **先预览后应用**：完整展示生成稿，确认后再应用；支持一步撤销，保留已有答卷
- **问卷设计**：编辑标题与说明，实时保存草稿
- **四种题型**：单选、多选、短文本、1–5 评分
- **题目管理**：新增、修改、必答开关、上下移动、选项管理和二次确认删除；“删除题目”与“确认删除”使用更大的文字及触控区域
- **实时预览**：设计页右侧同步展示受访者看到的问卷
- **URL 分享**：将规范化问卷编码进 Hash，复制链接即可跨设备打开
- **填写体验**：答题进度、必答校验、提交回执和再次填写
- **结果分析**：选择题比例、评分分布与均值、文字回答列表、答卷脉冲带
- **本地持久化**：问卷草稿与答卷保存在 localStorage
- **安全与恢复**：文本统一 HTML 转义，非法分享载荷和损坏存储自动回退
- **响应式与可访问性**：桌面/移动布局、键盘焦点、语义表单与减少动态效果

## 生成问卷

1. 在生成区填写调查主题、参考文本或提示词；受访人群可补充问卷的措辞背景。
2. 在题数框输入 4–30 范围内的任意整数。默认按内容自动匹配单选、多选、短文本或 1–5 评分；需要控制范围时，切换为手动限定并勾选题型。生成、编辑、保存和分享均支持最多 30 题。
3. 默认使用本地生成。若需要模型理解复杂要求，切换到 AI 模式，并填写 HTTPS Chat Completions 接口完整地址、模型名称和 API Key。
4. 生成后检查标题、说明、题目和选项，确认后应用到编辑器，再继续手动修改或分享。
5. 应用后的问卷使用新问卷 ID 和题目 ID。旧答卷保留在当前浏览器，不会混入新问卷统计；需要恢复原问卷时可立即使用一步撤销。

本地生成可以直接使用不带题型标签的提纲，例如：

```text
生成4题：
1. 年龄
2. 常用功能
3. 满意度
4. 改进建议
```

自动模式会把这四项依次生成为单选、多选、1–5 评分和短文本题，并补充相应问题措辞与选择题选项。也可以直接写“调查年龄、常用功能、满意度和改进建议”，或粘贴完整的问题。

常见内容与默认题型的对应关系如下；题目中的明确题型标签、已有选项，以及题型范围和数量要求会约束自动推断：

| 调查内容 | 默认题型 |
| --- | --- |
| 年龄、性别、学历、频率、是否、单项选择 | 单选 |
| 常用功能、使用场景、了解渠道等可选多项的内容 | 多选 |
| 满意度、推荐评分 | 1–5 评分 |
| 姓名、联系方式、原因、改进建议 | 短文本 |

提示词中的明确题数、各题型数量以及限定/排除题型优先于界面设置，例如“生成六道题，只要单选题，全部选答”或“共 12 题，6 道单选、4 道评分、2 道文本”。总题数范围为 4–30。可把现有题目和选项粘贴到提示词或参考文本中；生成时优先采用提示词中的明确题目与选项，再按要求补全剩余题目。明确的题型标签和选项优先于内容推断，例如：

```text
1. 您最近一个月就餐的频率是？ [单选]
A. 每周多次
B. 每周一次
C. 偶尔
2. 您希望优先改进什么？ [文本]
```

本地生成器使用关键词规则、文本要点与题型模板，适合快速搭建初稿，**不能完整理解任意提示词**。它能识别上述常见内容，但遇到含糊表达、否定或复杂组合要求时仍可能推断不准确。生成后请校对题型、选项完整性和问题是否中立；可以在提示词中明确标注题型，也可以应用后手动调整。AI 输出也会经过结构校验，再供用户预览。

参考文本和提示词不在输入、生成或刷新恢复时按应用预设字符数截断。生成输入保存在当前浏览器，方便刷新后继续；浏览器存储配额、设备资源和 AI 模型上下文容量仍然有限，长文本可能导致保存或服务请求失败，页面会显示相应错误。取消字符硬上限并不代表外部 AI 服务能处理任意长度的内容。

API Key 只留在当前页面内存，不写入本地存储或分享链接。AI 模式仅在用户主动点击生成时向所配置的服务发送本次主题、参考文本、提示词和受访人群等生成参数。请仅填写愿意交给该服务处理的内容；接口需支持浏览器跨域请求（CORS）。本地模式不发送生成内容。

删除题目采用两步确认：“删除题目”点击后变成“确认删除”，确认有效期为 10 秒；超时恢复原按钮。确认切换保留键盘焦点，便于继续操作。

## 静态分享边界

这个应用部署在 GitHub Pages，不包含数据库或服务端：

1. 分享链接携带的是问卷结构，因此题目可以跨设备打开。
2. 答卷保存在填写者当前浏览器，不会自动回传给创建者。
3. 结果页只统计当前浏览器中、问卷 id 相同的答卷。
4. 分享链接不包含已有答卷，也不包含生成输入或 API Key。分享和答卷功能不会上传答案；可选 AI 生成是单独的联网功能。

这个边界会在分享面板、填写页和结果页持续提示，避免把本地演示误解成云端问卷平台。

## 数据结构

~~~javascript
{
  survey: {
    id,
    title,
    description,
    questions: [{ id, type, title, required, options }]
  },
  responses: [
    { id, surveyId, submittedAt, answers: { [questionId]: value } }
  ]
}
~~~

## 视觉设计

界面取材自研究工作台和光学答题卡：深紫结构承载问卷逻辑，橙色表示正在传递的答案，青色和淡紫组成统计信号，柠檬黄用于选择与提示。结果页的“答卷脉冲带”让每份提交变成一个可见圆点，是贯穿整个应用的标志性元素。

## 技术栈

纯 HTML / CSS / JavaScript，无框架、无构建步骤。默认本地生成无需网络；可选 AI 模式使用浏览器 `fetch` 调用用户配置的 HTTPS Chat Completions 接口。

## 验证

- 生成器测试覆盖无标签提纲与自然语言内容的题型匹配、显式题型与选项优先级、手动限定与数量分配、长文本完整传递、4–30 题边界、生成结构与服务返回校验。
- 交互验证覆盖自动与手动题型模式、生成预览、应用、撤销、长文本刷新恢复、30 题编辑与分享、删除按钮可见性和二次确认，以及原有填写和统计流程。
- 移动布局检查覆盖 320px、390px 和 768px，并兼顾桌面展示。
- 根追踪器回归：`node --test qa/tracker-app038.test.js`。

运行 `node --test apps/038-online-survey/generator.test.js qa/*.test.js` 检查生成器和追踪器。浏览器回归需要 Playwright：在仓库根目录启动 `python -m http.server 8038 --bind 127.0.0.1`，再运行 `node --test apps/038-online-survey/browser.test.cjs`。可通过 `PULSE38_TEST_URL` 覆盖测试地址。

AI 自动化验证使用模拟服务，覆盖成功、非法结果、失败、取消与密钥隔离；真实服务的可用性取决于用户配置的地址、模型、密钥及 CORS 支持。

---
100 Apps Challenge · 2026-08-30 · Updated 2026-09-09 · App #038/100 · L3
